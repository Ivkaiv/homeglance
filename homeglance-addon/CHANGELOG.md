# Changelog — Homeglance Add-on

## 0.1.0-alpha.5

- Первая публичная версия HA Add-on. Запускает `ghcr.io/ivkaiv/{arch}-homeglance` через Supervisor.
- Поддерживаемые архитектуры: amd64, aarch64
- Автозапуск при старте HA, перезапуск при сбое
- Web-интерфейс на порту 3040
